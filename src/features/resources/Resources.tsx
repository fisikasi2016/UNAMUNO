import { resources } from '../../lib/mockData';
const blocks=['Clinics','Ariketen bankua','Prestaketa fisikoa','Lesioak','Planifikazioa','Unamuno'];
export function Resources({session}:{session:{role:string;teamId:string}}){return <section><div className="section-head"><h2>Baliabideak</h2><button>+ Baliabidea</button></div><div className="resource-menu">{blocks.map(b=><button key={b}>{b}</button>)}</div><input className="search" placeholder="Etiketak bilatu: erasoa, defentsa, lesioak..."/><div className="cards-grid">{resources.map(r=><article className="resource-card" key={r.id}><div className="thumb">▶</div><h3>{r.title}</h3><p>{r.description}</p><div className="tag-row">{r.tags.map(tag=><span key={tag}>#{tag}</span>)}</div>{(session.role==='coordinator'||session.teamId===r.authorTeamId)&&<button className="danger">Ezabatu</button>}</article>)}</div></section>}
